'use strict';

/** Canonical production origin for php777.pro — used by render + SEO guards. */
const SITE_ORIGIN = 'https://php777.pro';
const SITE_HOST = 'php777.pro';

module.exports = { SITE_ORIGIN, SITE_HOST };
